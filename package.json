{
  "name": "ts-multi-agents",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "dev:api": "npm --prefix apps/api run dev",
    "build:api": "npm --prefix apps/api run build",
    "start:api": "npm --prefix apps/api run start",
    "start:web": "npm --prefix apps/web run dev",
    "dev:web": "npm --prefix apps/web run dev",
    "build:web": "npm --prefix apps/web run build",
    "preview:web": "npm --prefix apps/web run preview",
    "test:judge": "npm --prefix apps/api run test:judge",
    "test:researcher": "npm --prefix apps/api run test:researcher",
    "test:loop": "npm --prefix apps/api run test:loop"
  },
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "skills": "^1.5.6"
  }
}
